{
  "name": "changeable-text-app",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Node.js app with text that changes when a button is clicked.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
