# Changeable Text App

A small Node.js application that displays text and changes it when a button is clicked.

## Run it

```powershell
npm start
```

Then open <http://localhost:3000>.

To change where the displayed text comes from, edit `getText()` in `public/script.js`.
